name: Publish to PyPI

on:
  release:
    types: [published]

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    environment:
      name: pypi
      url: https://pypi.org/p/neurodent

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v6
        with:
          python-version: '3.10'

      - name: Build package
        run: uv build

      - name: Verify version matches tag
        run: |
          PKG_VERSION=$(grep -m1 '^version = ' pyproject.toml | sed "s/^version = [\"']\(.*\)[\"']/\1/")
          TAG_VERSION="${GITHUB_REF_NAME#v}"
          echo "Package version: $PKG_VERSION"
          echo "Tag version:     $TAG_VERSION"
          if [ "$PKG_VERSION" != "$TAG_VERSION" ]; then
            echo "❌ Version mismatch! pyproject.toml=$PKG_VERSION, tag=$TAG_VERSION"
            exit 1
          fi
          echo "✅ Versions match!"

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          # TODO: After successful TestPyPI validation, remove the repository-url
          # line below to publish to production PyPI instead.
          repository-url: https://test.pypi.org/legacy/
